import styled from 'styled-components'

export const ScrollWrapperStyle = styled.section`
  margin-bottom: calc(30% - 1px);
  min-height: 100vh;
  position: relative;
  z-index: 1;
  margin-bottom: calc(30% - 1px);
  overflow-x: hidden;
  width: 100%;
`